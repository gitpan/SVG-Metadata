
svg_validate
   * Add option to suppress creation of .orig files
   * Add command to undo and move the .orig's back
   * Add option to generate the tagged files into a separate tree
   * Add options to enable specification of larger amount of metadata
